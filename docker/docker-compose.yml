version: '3.8'
services:
  backend:
    build: ../backend
    ports:
      - "8000:8000"
    env_file:
      - ../backend/.env
    depends_on:
      - tor-proxy

  tor-proxy:
    image: gold-standard/tor-proxy:latest
    ports:
      - "9050:9050"
